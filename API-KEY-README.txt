NOTE THAT OPENAI_API_KEY should be an environment variable in your system.

Example for MAC

~/.zshrc should have the line:

export OPENAI_API_KEY="sk-proj-xxxxxxxx"

You can test it with print $OPENAI_API_KEY